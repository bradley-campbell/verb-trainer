import React, { useContext } from "react";
import DisplayContext from "../DisplayContext";

const Welcome = () => {
  return (
    <div>
      <button></button>
      <button></button>
    </div>
  );
};

export default Welcome;
